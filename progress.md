# cJSON项目进度记录（STEP1）
- 今日任务：
  1. 完成cJSON核心结构体（struct cJSON）的深度注释，理解双向链表+树形结构的设计逻辑；
  2. 解决“找不到parse object函数”的问题,定位cJSON解析总入口函数cJSON_Parse；
  3. 给cJSON_Parse函数添加核心流程注释，明确JSON解析的整体逻辑；
  4. struct cJSON通过next/prev（双向链表）+ child（树形）实现JSON嵌套结构；
  5. cJSON_Parse是解析总入口，会根据JSON首字符调用不同类型的解析函数；
## Day2（找到并注释解析入口函数）
  1. 用搜索命令找到cJSON_Parse函数的实现代码，这是解析JSON字符串的入口；
  2. 给这个函数加了简单注释，理解了它的作用：接收JSON字符串，返回解析结果；
  3. 发现这个函数内部会调用另一个解析函数，是简化版的使用方式；
  4. 学会了用nano跳行号、用grep搜索代码的小技巧。
## Day3（找到并注释底层解析函数）
  1. 用搜索命令找到cJSON_ParseWithOpts函数（行号xxx），它是cJSON_Parse的底层实现；
  2. 给这个函数加了简单注释，知道它有3个参数，比cJSON_Parse更灵活；
  3. 学会了调整搜索命令，适配不同的代码格式；
  4. 熟练了打开WSL并一键进入cJSON目录的技巧。
## Day4（理解C语言内存释放函数）
  1.找到cJSON_Delete函数，它是用来释放JSON解析后占用的内存；
  2.给这个函数加了简单注释，理解大一重点：C语言用完内存要手动释放（避免内存泄漏）；
  3.知道这个函数的参数是cJSON_Parse返回的解析结果，无返回值；
  4.继续熟练“找函数→加注释→Git推送”的完整流程。
## Day5（理解JSON解析+输出的完整流程）
  1.找到cJSON_Print函数，它是cJSON_Parse的逆操作（结构体→字符串）；
  2.给这个函数加了简单注释，理解“解析JSON字符串→处理→转回字符串”的完整流程；
  3.巩固大一C语言“输入→处理→输出”的核心编程逻辑；
  4.完全熟练Git的add/commit/push提交流程，能独立完成每日进度更新。
## 5天核心学习总结（Day1-Day5）
  1.环境搭建：配置WSL+Git+SSH，学会将代码推送到GitHub，掌握每日提交流程；
  2.核心函数：理解cJSON解析（Parse/ParseWithOpts）、内存释放（Delete）、输出（Print）的核心逻辑；
  3.C语言知识点：巩固“函数参数/返回值”“内存释放（避免泄漏）”“输入→处理→输出”等大一核心考点；
  4.实用技能：学会用grep搜索代码、nano跳行号，能独立完成源码注释和进度记录。
## Day6（拓展输出函数）
  1.找到cJSON_PrintUnformatted函数，理解它和cJSON_Print的区别：格式化输出vs无格式输出；
  2.对比学习两个函数，掌握“同一功能不同形式”的编程思路（大一易理解）；
  3.汇总5天核心收获，形成完整的cJSON基础学习路径。
## Day7（实战测试cJSON核心流程）
  1.新建test_cJSON.c文件，写极简测试代码，验证“解析→输出→释放”的完整流程；
  2.代码调用了前6天注释的Parse/Print/Delete函数，形成学习闭环；
  3.巩固大一C语言“头文件引用”“条件判断”“内存释放”等基础知识点；
  4.从纯源码注释转向简单实战，理解开源库的实际使用方式。
